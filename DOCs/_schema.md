# Configuration schema (documentation hub)

**Specification:** field rules, execution-token syntax, and intercept behavior for `workspaces.json` are fully documented in **[Configuration.md](Configuration.md)**.

**Readme entry point:** a collapsible index of related docs (and the same cross-links in one place) lives in the repository readme:

**[Configuration schema reference →](../readme.md#configuration-schema-reference)**

Use this `_schema.md` file when linking from other files under `DOCs/` so paths stay simple (`_schema.md` vs `../readme.md#…`) and only this hub needs updating if the readme anchor or layout changes.
